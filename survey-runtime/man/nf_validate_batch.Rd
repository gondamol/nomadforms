% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{nf_validate_batch}
\alias{nf_validate_batch}
\title{Validate Batch}
\usage{
nf_validate_batch(responses, rules, labels = NULL)
}
\arguments{
\item{responses}{Named list of responses}

\item{rules}{Named list of validation rules for each field}

\item{labels}{Named list of labels for each field}
}
\value{
List with valid (TRUE/FALSE), errors list, and summary
}
\description{
Validates multiple responses at once
}
\examples{
responses <- list(email = "user@example.org", age = 15)
rules <- list(
  email = list(required = TRUE, type = "email"),
  age = list(required = TRUE, min = 18)
)
nf_validate_batch(responses, rules)
}
