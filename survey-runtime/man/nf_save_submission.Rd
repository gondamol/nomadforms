% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{nf_save_submission}
\alias{nf_save_submission}
\title{Save a Completed Survey Submission}
\usage{
nf_save_submission(
  conn,
  survey_id,
  session_id,
  responses,
  participant_id = NULL,
  project_id = NULL,
  device_info = NULL,
  response_id = NULL
)
}
\arguments{
\item{conn}{DBI connection object}

\item{survey_id}{Survey identifier (text; matches the PWA's survey id)}

\item{session_id}{Session identifier}

\item{responses}{Named list of question ids to values}

\item{participant_id}{Optional participant identifier}

\item{project_id}{Optional project UUID this survey belongs to}

\item{device_info}{Optional named list of device metadata}

\item{response_id}{Optional client-supplied UUID (enables idempotent upsert)}
}
\value{
Submission UUID
}
\description{
Stores one completed form as a single document row in `submissions`. The
whole set of answers is the unit that is written and synced, which is what
makes offline retries idempotent.
}
\examples{
\dontrun{
conn <- nf_database(connection_string = Sys.getenv("DATABASE_URL"))
nf_save_submission(
  conn,
  survey_id = "household_survey",
  session_id = "session-001",
  responses = list(age = 34, county = "Turkana")
)
}
}
