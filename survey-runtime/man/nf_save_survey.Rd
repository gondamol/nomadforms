% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage.R
\name{nf_save_survey}
\alias{nf_save_survey}
\title{Save a Survey Submission (with connection handling)}
\usage{
nf_save_survey(
  survey_id,
  session_id,
  responses,
  participant_id = NULL,
  project_id = NULL,
  device_info = NULL,
  response_id = NULL,
  conn = NULL
)
}
\arguments{
\item{survey_id}{Survey identifier (text)}

\item{session_id}{Session identifier}

\item{responses}{Named list of question ids to values}

\item{participant_id}{Optional participant identifier}

\item{project_id}{Optional project UUID}

\item{device_info}{Optional named list of device metadata}

\item{response_id}{Optional client-supplied UUID (idempotent upsert)}

\item{conn}{Optional existing connection; one is created if omitted}
}
\value{
List with success status and the submission id
}
\description{
Convenience wrapper around [nf_save_submission()] that manages the database
connection. The full set of answers for one completed form is written as a
single document row.
}
\examples{
\dontrun{
nf_save_survey(
  survey_id = "household_survey",
  session_id = "session-001",
  responses = list(age = 34, county = "Turkana")
)
}
}
